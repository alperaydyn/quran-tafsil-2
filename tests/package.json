{
  "name": "tafsil-tests",
  "version": "1.0.0",
  "private": true,
  "description": "tafsil.net — QA & Integration Test Suite (Agent-05)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:data": "vitest run tests/data/",
    "test:api": "vitest run tests/api/",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.11.24",
    "@types/pg": "^8.11.2",
    "@types/supertest": "^6.0.2",
    "dotenv": "^16.4.5",
    "pg": "^8.11.3",
    "supertest": "^7.0.0",
    "typescript": "^5.3.3",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
